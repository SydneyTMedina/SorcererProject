#include "Map.h"
#include <iostream>
#include <cmath>
#include <iomanip>
#include <string>
#include <cassert>
#include <fstream>
using namespace std;

int main() {
    Map map = Map();
    /*
    map.fillMap();
    string input="";

    while(input!="q")
    {
     map.displayMap();
     cout<<"Enter your move (a,s,w,d. q to quit): "<<endl;
     cin>>input;
    map.executeMove(input.at(0));
    }
    */
    
}